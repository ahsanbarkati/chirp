query getUserFollowers($username: String!) {
  getUser(username: $username) {
    followers {
      name
      username
      info
    }
    followedUsers {
      name
      username
      info
    }
    
  }
}